Implement lean production cleanup for Owner CRM and Venue onboarding. No unrelated refactors.

1. DATABASE / SCHEMA LINK

Modify lib/db/src/schema/owner-leads.ts

Add nullable:

venueId uuid references venues.id

Keep existing fields untouched.

Update any exported TS types accordingly.

2. API SCHEMA CONTRACT FIX

In generated/shared API schemas where OwnerLeadStatus is declared:

replace status union with ONLY:

"new" | "qualified" | "onboarded" | "rejected"

(remove contacted/demo everywhere)

Also expose optional venueId?: string | null on OwnerLead interface.

No other contract changes.

3. ADMIN BACKEND CONVERT ROUTE

Create in artifacts/api-server/src/routes/admin.ts (or existing admin owner lead routes):

POST /api/admin/owner-leads/:leadId/convert

Behavior:

requireAdmin
fetch owner_lead by id
reject if not found or already has venueId
create a new venue row prefilled from lead data using best available mappings:
name from venueName/companyName/title available in lead
location/address/locality/city from lead fields
sportSupported from lead preferredSport if exists
ownerName/contact from lead
isApproved = false
ownerUserId = null initially
carry sane placeholder defaults for required venue columns
set owner_leads:
venueId = createdVenue.id
status = "onboarded"

Return:
{
success: true,
venueId,
venueName
}

Single transaction.

4. ADMIN VENUE ACTIVATE ROUTE

Add:

POST /api/admin/venues/:venueId/activate

Behavior:

requireAdmin
set venue.isApproved = true
call existing backfillVenuePricingDefaults() only for this venue if needed
call regenerateVenueSlotsForNext14Days() only for this venue
return activated true

Do not duplicate utility logic if reusable helper can be imported.

5. ADMIN FRONTEND OWNER CRM TAB CLEANUP

Find Owner CRM admin page in matchpit frontend.

Make these UI changes only:

statuses shown in dropdown/buttons:
new
qualified
onboarded
rejected

remove contacted/demo.

Add action button per lead:

Convert To Venue

Visible when:

status is new or qualified
venueId absent

Calls /api/admin/owner-leads/:id/convert

After success:

refresh table
show linked venue id/name badge

If venueId exists:
show small "Open Venue" action.

6. ADMIN FRONTEND VENUES TAB CLEANUP

For venues where isApproved=false, show:

Activate Venue

Calls /api/admin/venues/:venueId/activate

After success:
venue becomes approved/live.

No manual approve/reject boolean confusion.

IMPORTANT

No broad redesign.
No CSS beautification.
Only functional admin workflow cleanup.

Run:
tsc --noEmit -p artifacts/api-server/tsconfig.json

Return:

modified files
any typecheck issues
endpoint summary