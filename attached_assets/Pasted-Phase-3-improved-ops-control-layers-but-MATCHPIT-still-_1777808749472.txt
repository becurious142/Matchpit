Phase 3 improved ops/control layers, but MATCHPIT still has final consumer-critical gaps.

I need PHASE 4 FINAL CONSUMER COMPLETION + AUTOMATION HARDENING.

Do not do cosmetic placeholders. Implement production-grade transactional continuity.

========================

FULL NOTIFICATION CENTER
========================

Build end-to-end notification system, not just enum values:

notifications table with read/unread
backend notification creation helpers
auto-create notifications on:
reserve success,
match almost full,
match confirmed,
final payment due,
final payment overdue,
player dropped unpaid,
underfilled cancelled,
wallet refund credited,
referral reward earned

Frontend:

topbar notification bell with unread badge
notifications page/dropdown
mark as read
deep-link CTA per notification
========================
2. WAITLIST / SPOT REOPEN ENGINE

Implement true refill automation:

when confirmed participants fail final payment or cancel,
reopen those spots until a refill cutoff
mark match badge "Spots Reopened"
allow new reserve joins if financially safe
update host finance summary accordingly
prevent venue payout loss
========================
3. WALLET ECONOMY UI COMPLETION

Build wallet as visible consumer currency:

dedicated wallet history page
ledger list with labels (refund, cashback, referral, bonus, debit)
wallet balance card
auto-use wallet toggle
payment screen wallet banners
“refund credited to wallet” confirmation surfaces
========================
4. PAYMENT FAILURE / PENDING RECOVERY

Implement pending payment recovery:

store pending Razorpay intents
if browser closes before verify, user can retry verify
payment failed banners
abandoned pending payment reminders
idempotent reconciliation route for stuck orders

Apply to:

private booking
host commitment
reserve join
final payment
========================
5. POST TRANSACTION SUCCESS STATES

Build polished success/follow-up modals/screens:

booking successful
hosted match created
reserve spot success
final payment success
refund success
referral earned

Each should include next best CTA:
share, invite, view wallet, browse matches, host again.

========================
6. MATCH SHARE ATTRIBUTION REWARDS

Implement tracked match-share referrals:

generate referralized match join links
if invited friend signs up and pays reserve/booking, reward inviter wallet
host squad fill bonus if host fills all spots through invites
reward events + QA coverage
========================
7. JAIPUR HOMEPAGE PREMIUMIZATION

Polish consumer homepage and discovery:

Jaipur locked launch branding
premium hero section
trust proof counters
wallet promo banner
top live matches carousel
top venues by sport
owner CTA footer
startup-grade empty states
========================
8. HARD RULES
preserve all existing commerce and QA
extend QA for all new systems
typecheck clean
no mocks/no TODOs/no placeholders

At the end provide:

exact files changed
exact schema changes
exact new routes
QA count
deploy commands