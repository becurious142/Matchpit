Excellent. Phase 2 foundation is complete.

Now continue implementation with NO summaries until all remaining commerce-critical layers are built.

The current repository still lacks the deep transactional systems required for actual launch.

You must now execute the following remaining phases fully.

====================================================
PHASE 3 — COMPLETE MATCHPIT WALLET TRANSACTION ENGINE
=====================================================

Do not leave wallet as admin-only references.

Build full production wallet system:

Required tables if not fully implemented:

* wallet_accounts
* wallet_ledger
* reward_events

Required capabilities:

1. wallet balance per user
2. admin manual credit/debit
3. signup bonus credit support
4. referral reward credit support
5. first booking cashback
6. first hosted match cashback
7. underfilled match cancellation refund to wallet option
8. private booking cancellation refund support
9. wallet debit during:

   * private booking checkout
   * hosted match reserve payment
   * final match payment
10. wallet auto-use toggle per user

Frontend required:

* wallet card on dashboard
* wallet history page
* wallet apply toggle on all payment dialogs

Backend:
all payment verification routes must ledger wallet movements if wallet used.

====================================================
PHASE 4 — REFERRAL REWARD ENGINE
================================

Referral code display alone is insufficient.

Implement complete referral lifecycle:

* every user has unique referral_code
* referral attribution on signup
* referred_by column on profiles
* first successful paid transaction by referred user triggers:
  referrer reward
  referee reward
* rewards go to wallet ledger
* prevent duplicate rewarding

Admin:
referral reward config values adjustable

====================================================
PHASE 5 — BADGE + TRUST + RELIABILITY ENGINE
============================================

Implement computed trust system.

Need:

* user_stats table or computed view
* badges based on completed bookings, hosted matches, payment reliability, no-show ratio, cancellation ratio

Badges:
Early Player
Reliable Player
Match Regular
Power Host
Fair Host
Verified Organizer
No Show Risk

Display badges:

* profile page
* hosted match participant list
* host card
* leaderboard/admin user screen

====================================================
PHASE 6 — HOSTED MATCH COMMERCE CLOSURE ENGINE
==============================================

This is critical and must be transaction-safe.

Audit current hosted match commerce and fully close missing cases:

A. if minimum_players_required not reached by lock deadline:

* mark match cancelled_underfilled
* trigger refunds for host commitment + reserve fees
* refund route supports wallet credit or source refund placeholder
* ledger all refunds

B. if match confirmed:

* final payment reminders generated
* participant final payment state tracked

C. if participant fails final payment before deadline:

* mark participant dropped_unpaid
* recompute payable split if needed

D. host no-show / manual cancellation penalty placeholders

E. participant no-show penalty placeholders

Need cron-safe automated status transitions.

Add:
Admin > Hosted Match Finance Inspector page

====================================================
PHASE 7 — VENUE PAYOUT AUTOMATION ENGINE
========================================

Finance dashboard UI is not enough.

Build transactional venue payout generation.

On every completed:

* private booking
* hosted match

system must generate venue_payout_ledger row with:
gross_collected
gateway_fee
platform_commission
venue_payable
linked_transaction_ids
payout_status

Also generate platform_revenue_ledger rows automatically.

Admin payout page should reflect real generated data, not manual-only records.

====================================================
PHASE 8 — OWNER CRM HARDENING
=============================

Expand owner CRM into real operational CRM:

* assigned admin
* followup notes log
* KYC document placeholders
* onboarding stage tracker
* expected monthly inventory value
* commission agreement status
* next action date

Add kanban style admin board.

====================================================
PHASE 9 — 100+ COMMERCE QA HARNESS
==================================

Expand QA to at least 100 assertions including:

wallet credit/debit,
wallet checkout deductions,
referral rewards,
coupon scope,
host refunds,
reserve refunds,
underfill cancellation,
venue payout generation,
platform revenue generation,
badge assignment,
cron state transitions,
duplicate payment prevention,
idempotent verify callbacks.

Run full QA and fix all failures.

====================================================
PHASE 10 — FINAL INSTRUCTIONS
=============================

Do not stop at schema creation.

You must:

* wire backend
* wire frontend
* wire admin
* wire ledgers
* wire cron transitions
* run typecheck
* run QA
* fix all diagnostics

Then provide a complete file-by-file execution report.

Do not summarize prematurely.
Continue autonomous implementation now.
