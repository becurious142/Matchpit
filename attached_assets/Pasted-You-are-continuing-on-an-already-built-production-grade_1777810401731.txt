You are continuing on an already-built production-grade MATCHPIT monorepo.

IMPORTANT:
Do not rebuild existing systems.
Audit current codebase first and extend only where needed.

Already completed and must remain intact:

private venue booking commerce
hosted match commerce with reserve/final payment
wallet ledger
referral engine
badges
trust score
owner panel
admin live ops
notifications center
payout ledgers
finance dashboards
city master with Jaipur active
coupons
underfill refunds
drop spot engine
payment recovery
QA suite 162/162

You are now building ONLY the missing founder-critical retention/community/launch systems.

This is PHASE 5.

Build everything fully integrated with existing DB, API, frontend, admin, notifications, QA, and payment architecture.

================================
MODULE 1 — JAIPUR ONLY LAUNCH POLISH
================================

We are launching only in Jaipur.

Rules:

if only one city is active in city_master, hide city selector from all user-facing pages
silently auto-assign Jaipur context
all venue queries, match queries, feed queries should automatically filter active city only
no blank/non-operational cities visible anywhere

Admin still keeps city CRUD for future expansion.

Also add Jaipur launch branding blocks on homepage:

"Jaipur's Sports Community"
live counters: venues, matches hosted, players joined, wallet rewards distributed

Counters should be DB driven.

================================
MODULE 2 — SOCIAL COMMUNITY FEED (SPORTS ADDA)
================================

Build a complete in-app sports community feed.

New DB tables required:

community_posts

id
user_id
city_id
type (text,image,looking_players,match_result,challenge,venue_review,achievement)
caption
image_url nullable
related_match_id nullable
related_venue_id nullable
related_squad_id nullable
sport
likes_count
comments_count
created_at

community_post_comments

id
post_id
user_id
comment
created_at

community_post_likes

id
post_id
user_id

Required backend routes:

GET /community/feed
POST /community/post
POST /community/comment
POST /community/like
DELETE /community/like

Feed ordering logic:

same city first
recent first
high engagement boosted

Users can create:

text sports update
looking for players tonight
won match today
challenge callout
venue review
image post

Frontend:

new page /community

premium card feed UI:
profile avatar
trust badge
time ago
sport chip
caption
image if any
like/comment/share row

post composer sticky top.

Also show community preview section on homepage.

IMPORTANT:
Feed must never look empty.

So system must auto-create achievement posts from existing platform events:

user hosted nth match
user earned badge
match confirmed
squad won challenge
venue crossed booking milestone

Create helper:
createSystemAchievementPost()

and wire this into existing payment/booking/badge/match confirmation callbacks.

================================
MODULE 3 — SQUADS / GROUPS ENGINE
================================

Build local sports squads.

DB:

squads

id
name
logo_url
city_id
sport
captain_user_id
description
wins
losses
trust_rating
created_at

squad_members

id
squad_id
user_id
role (captain,member)
joined_at

squad_posts

id
squad_id
user_id
message
created_at

Routes:

POST /squads/create
GET /squads
GET /squads/:id
POST /squads/:id/join
POST /squads/:id/post
POST /squads/:id/leave

Frontend:

/squads
/squads/:id

Features:

create squad
join squad
squad wall
members list
captain badge
trust score average
wins/losses
challenge another squad CTA

Homepage should show "Popular Jaipur Squads".

================================
MODULE 4 — PLAYER FOLLOW NETWORK
================================

DB:

player_follows

id
follower_user_id
following_user_id
created_at

Routes:

POST /profile/:id/follow
POST /profile/:id/unfollow
GET /profile/:id/network

Profile page enhancements:

followers count
following count
frequent teammates count
follow button
invite to match CTA

This creates personal sports graph.

================================
MODULE 5 — MATCH CHAT / COORDINATION WALL
================================

Need lightweight communication inside every hosted match.

DB:

match_messages

id
match_id
user_id
message
created_at

Routes:

GET /hosted-matches/:id/chat
POST /hosted-matches/:id/chat

Frontend:

inside match-detail page add tab:
Discussion

Participants can post:

reaching in 10 mins
bring balls
I invited one more guy
parking info

No websocket required.
Simple polling every 10 sec.

Unread message notification type required.

================================
MODULE 6 — SQUAD CHALLENGE ENGINE
================================

Allow one squad to challenge another squad.

DB:

squad_challenges

id
challenger_squad_id
opponent_squad_id
proposed_date
proposed_slot_id nullable
sport
status (pending,accepted,rejected,completed)
hosted_match_id nullable
created_at

Routes:

POST /squads/challenge
POST /squads/challenge/:id/respond
GET /squads/challenges

When accepted:

auto create hosted match in existing hosted_matches table
linked to both squads.

winner can later be marked from admin or captain panel.

Challenge acceptance should auto-create system achievement/community post.

================================
MODULE 7 — AUTO SOCIAL ACHIEVEMENT ENGINE
================================

Build centralized helper:

lib/social-events.ts

Functions:

createAchievementPostForBadge
createAchievementPostForHostedMatchMilestone
createAchievementPostForMatchConfirmed
createAchievementPostForReferralMilestone
createAchievementPostForSquadChallengeWin

These should insert achievement-type community_posts automatically.

Wire into:

badges awarding
hosted match confirmations
referral rewards
owner milestones
challenge completion

This ensures feed always alive even with low user generated content.

================================
MODULE 8 — TESTER INVITE + WAITLIST CONTROL
================================

We have only 3-4 testers initially.

Need admin tester tools:

test_invites table:

id
phone
name
invite_code
status
notes
created_at

Routes:
POST /admin/testers/add
GET /admin/testers
POST /admin/testers/mark

Optional:
During signup allow invite code field but not mandatory.

Admin can track who tested what.

Also add quick admin buttons:
"seed demo community posts"
"seed demo squads"
"seed demo notifications"

because app should not look empty on first demo.

================================
MODULE 9 — VERCEL/PRODUCTION DEPLOY READINESS
================================

Prepare app for deployment:

audit all env usage
create .env.example complete
ensure frontend api base url configurable
remove any localhost assumptions
ensure Neon SSL config production safe
create production build script
create vercel.json if required
create DEPLOYMENT.md with exact steps

Do not deploy.
Only prepare fully.

================================
MODULE 10 — QA EXPANSION
================================

Expand QA suite from 162 checks to 250+ checks.

Must include:

community integrity
squad integrity
follow graph integrity
chat integrity
challenge integrity
achievement post generation
Jaipur single-city visibility invariants
notification deep links
production env checks

Run:

pnpm run typecheck
QA suite full pass mandatory.

Do not stop at partial implementation.

DELIVERY FORMAT REQUIRED

When complete provide:

every file modified/created
every new DB table
every new route
every frontend page/component
QA final score
exact commands remaining for me
any env vars needed
any manual Neon SQL migration if required

No placeholders.
No pseudo completion.
Implement fully.