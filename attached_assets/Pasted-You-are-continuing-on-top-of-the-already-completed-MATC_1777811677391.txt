You are continuing on top of the already completed MATCHPIT Phase 1–5 codebase.

Do not rebuild prior modules.

Already completed:

bookings commerce
hosted match commerce
wallet/referral/badges/trust
owner/admin ops
payouts/finance
community feed
squads
follow graph
match chat
tester invite tools
Jaipur-only city logic
QA 256/256

Now build ONLY the missing founder launch infrastructure required before public beta deployment.

Everything must be fully production-minded and integrated.

=========================================
MODULE 1 — OFF-APP COMMUNICATION AUTOMATION (WHATSAPP/SMS READY)
=========================================

We need communication beyond in-app notifications.

Create notification dispatch abstraction:

lib/dispatch.ts

Supported channels:

in_app (existing)
whatsapp (prepare abstraction)
sms (prepare abstraction)

Do not hardcode external provider yet, but architecture must support MSG91/Twilio later.

Create table:

notification_dispatch_logs

id
user_id
notification_id nullable
channel
destination
template_key
payload jsonb
status (queued,sent,failed)
created_at

Create helper:
dispatchNotification(userId, templateKey, payload, preferredChannels)

For now:

in_app sends existing notification row
whatsapp/sms create queued logs only (simulate provider ready state)

Wire dispatchNotification into:

booking payment success
host commitment success
reserve join success
final payment due
match confirmed
match almost full
host nudge unpaid
squad challenge accepted
referral reward earned
wallet refund issued

Admin page should show Notification Dispatch Logs tab.

=========================================
MODULE 2 — ANALYTICS EVENT ENGINE + FOUNDER FUNNELS
=========================================

Create table:

analytics_events

id
user_id nullable
event_name
meta jsonb
created_at

Create helper:
trackEvent(eventName, userId?, meta?)

Wire into ALL critical actions:

signup_started
signup_completed
referral_applied
booking_started
booking_paid
host_match_started
host_match_paid
reserve_join_started
reserve_join_paid
final_payment_started
final_payment_paid
wallet_used
coupon_applied
community_post_created
squad_created
squad_joined
follow_user
chat_message_sent
notification_clicked

Admin dashboard:
new tab "Funnels"

Show:

signups today/week
signup→booking %
host started→paid %
reserve started→paid %
reserve→final payment %
community engagement %
referral conversion count
wallet usage count

Must be DB-driven.

=========================================
MODULE 3 — DEMO LAUNCH SEEDER / EMPTY APP MASKING
=========================================

Need realistic synthetic Jaipur activity.

Create admin seed commands:

POST /admin/seed/demo-profiles
POST /admin/seed/demo-community
POST /admin/seed/demo-squads
POST /admin/seed/demo-hosted-matches
POST /admin/seed/demo-notifications
POST /admin/seed/demo-challenges

Seeder requirements:

40 Jaipur demo users with varied sports/areas/trust scores
30 community posts
10 squads
15 hosted matches in mixed statuses
20 notifications
5 squad challenges

All data must look realistic:
Mansarovar, Vaishali, Malviya Nagar, Jagatpura, Raja Park, etc.

Use Indian names.

This is critical so app never looks empty in beta.

Admin should get one-click "Seed Full Demo Ecosystem".

=========================================
MODULE 4 — ABUSE / FRAUD / MODERATION CONTROLS
=========================================

Need protection systems.

Create tables:

user_reports

id
reporter_user_id
target_type (user,post,squad,chat)
target_id
reason
created_at
status

user_strikes

id
user_id
type (spam,drop_abuse,referral_abuse,no_show,report)
points
notes
created_at

Rules to implement:

max 5 community posts/day/user
max 3 hosted matches/day/user
max 10 chat messages in 5 minutes
if user drops 3 reserved matches in 30 days => strike
if same device/self referral suspected => strike flag (basic email/phone heuristic acceptable)
report buttons on post/squad/chat/profile

Admin moderation tab:

reports queue
user strikes
suspend user quickly

Integrate with existing isSuspended.

=========================================
MODULE 5 — RAZORPAY PAYMENT HARDENING + WEBHOOK RESILIENCE
=========================================

Current test keys not yet added.

Need payment hardening.

Create:

POST /payments/webhook

Handle Razorpay webhook events:

payment.captured
payment.failed
order.paid

Verify webhook signature.

Idempotently reconcile pending payments.

Mark payment rows accordingly.

Ensure duplicate callbacks do not duplicate:

wallet credits
referral rewards
payout ledger entries
participant payment confirmations

Add admin tool:
GET /admin/payments/reconcile-pending

to inspect all stale pending payments.

Also create dev simulation script:
scripts/src/payment-smoke.ts

which runs:
host payment → reserve payment → final payment → booking payment logical smoke validation.

=========================================
MODULE 6 — GUIDED FIRST USER ONBOARDING FUNNEL
=========================================

Current signup is too cold.

Build post-signup onboarding wizard.

After signup/login first time if profile.onboardingComplete=false:

show onboarding flow:

Step 1 choose favorite sports
Step 2 choose preferred Jaipur areas
Step 3 choose skill level
Step 4 follow suggested players OR join suggested squad
Step 5 show welcome wallet credit/referral code
Step 6 show recommended nearby matches

Then mark onboardingComplete=true.

Need DB additions in profiles:

onboarding_complete boolean
preferred_areas text[]
primary_skill_level text

This should massively improve first-session activation.

=========================================
MODULE 7 — FOUNDER KPI SUPER DASHBOARD
=========================================

Admin panel final tab: Founder KPI

Show:

total GMV
wallet liabilities
unpaid payouts
active users 7d
matches created this week
reserve conversion %
final payment conversion %
top venues
top hosts
top referral users
top engaged community posts
suspicious users count
notification dispatch sent count

This should be one-glance founder command center.

=========================================
MODULE 8 — PRODUCTION ENV PREP FOR VERCEL + NEON
=========================================

Create:

DEPLOYMENT.md
VERCEL_DEPLOY.md

Need:

all required env vars documented
frontend API URL env
Neon SSL production connection verified
CORS production origin handling
no localhost assumptions
healthcheck endpoint verified
vercel.json if needed

Do not deploy, only prepare.

=========================================
MODULE 9 — QA EXPANSION TO 340+
=========================================

Expand QA from 256 to 340+.

Must include:

dispatch log integrity
analytics event integrity
demo seed integrity
moderation/report integrity
webhook/payment idempotency invariants
onboarding profile invariants
founder KPI query integrity
env/config readiness

Run full typecheck.

No partial completion.

REQUIRED DELIVERY FORMAT

When done provide:

all files modified/created
all DB schema changes
all new endpoints
all frontend pages/components changed
QA final score
exact env vars now required
exact commands left for me
any manual Neon SQL steps

No placeholders.
No pseudo done.
Implement fully.